name: Terraform Apply on Dev environment

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  terraform-apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main repository
        uses: actions/checkout@v3

      - name: Run terraform apply on dev
        shell: bash
        run: |
          echo "Run terraform apply on dev"
          